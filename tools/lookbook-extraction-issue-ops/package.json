{
  "name": "lookbook-extraction-issue-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "lookbook-extraction-issue-ops": "src/index.js"
  },
  "scripts": {
    "build": "node --check src/index.js && node --check src/arguments.js && node --check src/client.js && node --check src/config.js && node --check src/gcloud.js",
    "lint": "npm run build",
    "test": "node --test src/*.test.js"
  },
  "engines": {
    "node": "24"
  }
}
